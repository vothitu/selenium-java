package org.example;

import tvn.day1.Fraction;

import java.util.Arrays;
import java.util.Comparator;
import java.util.List;
import java.util.stream.IntStream;

// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {
//        Fraction f1 = new Fraction(  1,  2);
//        Fraction f2 = new Fraction( 1, 3);
////        Fraction f= f1.Sum(f2);
////        f.Sum(f);
//        f1.compare(f2);
//
//        int[] data = {16, 28, -16, -4, -13, 11, 28, 8, 10, 21, 4, 30, -7, -14};
//        List<Integer> intArr = Arrays.asList(data);
//        IntStream.range(0, intArr.size())
//                .boxed().max(Comparator.comparing(intArr::get))
//                .ifPresent(ix->System.out.println("Index "+ix+", value "+intArr.get(ix)));
//
//        IntStream.range(0, intArr.size())
//                .boxed().min(Comparator.comparing(intArr::get))
//                .ifPresent(ix->System.out.println("Index "+ix+", value "+intArr.get(ix)));
//        int max = data[0];
//        int maxIndex = 0;
//
//        for (int i = 0; i < data.length; i++) {
//            if (data[i] > max) {
//                max = data[i];
//                maxIndex = i;
//            }
//        }
//        System.out.println(max);
//        System.out.println(maxIndex);
//        int min = data[0];
//        int minIndex = 0;
//        for (int i = 0; i < data.length; i++) {
//            if (data[i] < min) {
//                min = data[i];
//                minIndex = i;
//            }
//
//        }
//        System.out.println(min);
//        System.out.println(minIndex);
    }

}